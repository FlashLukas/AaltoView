"""aaltoview -- look at AaltoFlow measurements (.nc), export them anywhere.

    python -m aaltoview [file.nc] [--folder DIR]

No Qt outside `apps`: data (reading files), view (reducing an N-D cube to a map
or a line), export (files, figures, notebooks), origin (a running Origin).
"""

__version__ = "0.1.0"
